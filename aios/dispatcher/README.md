# AIOS Agent Dispatcher

The dispatcher specification resolves a user request into a workflow, required skills, a model alias and a compatible runtime adapter.

It does not bypass approval, permission, cost, retry, validation or rollback controls.

Current state: production specification. Runtime implementation and unit tests are the next sprint.
